/**
 * Game constants
 */
class Constants {
  // Default element dimensions
  static DEFAULT_ELEMENT_WIDTH = 16;
  static DEFAULT_ELEMENT_HEIGHT = 16;

  // Character dimensions and collision
  static CHARACTER_WIDTH = 48;
  static CHARACTER_HEIGHT = 48;
  static CHARACTER_COLLISION_OFFSET_X = 16;
  static CHARACTER_COLLISION_OFFSET_Y = 24;
  static CHARACTER_COLLISION_WIDTH = 14;
  static CHARACTER_COLLISION_HEIGHT = 12;

  // Movement and speed
  static DEFAULT_MOVE_SPEED = 300;
  static DEFAULT_VIEWPORT_SPEED = 300;

  // Area coordinates validation
  static MAX_AREA_COORDINATE = 1000;
}
